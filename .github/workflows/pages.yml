# examples/report/-г GitHub Pages дээр нийтэлнэ (жишээ тайланг suulgалгүй үзэх).
# Ажиллахын тулд: Settings → Pages → Source: GitHub Actions (нэг удаа).
name: Pages

on:
  push:
    branches: [master, main]
    paths:
      - "examples/report/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: examples/report
      - id: deployment
        uses: actions/deploy-pages@v4
